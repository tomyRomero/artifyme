EXPO_PUBLIC_FAST_API_URL=http://172.20.10.4:8000
# The IP address of the same network my server and device using (iphone) are sharing ,
# the port number of where the sever uvicorn for python is running
EXPO_PUBLIC_JAVA_API_URL=http://172.20.10.4:8080
# The IP address of the same network my server and device using (iphone) are sharing ,
# the port number of where the sever tomcat for java is running
